{% extends 'backend/base.html.twig' %}

{% block pageContent %}
    
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Nowy kraj
        </h1>
        <ol class="breadcrumb">
            <li>
                <i class="glyphicon glyphicon-dashboard"></i>  
                <a href="{{ path('admin_homepage') }}">Kokpit</a>
            </li>            
            <li>
                <i class="glyphicon glyphicon-globe"></i> 
                <a href="{{ path('admin_countries_list') }}">Zarządzanie krajami</a>
            </li>
            <li class="active">
                <i class="fa fa-file"></i>
                Nowy kraj
            </li>
        </ol>    
    </div>
</div>     

<div class="row">

    <div class="col-lg-12">
            
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="form-group">
                <div class="col-sm-2"></div>
                <div class="col-sm-10">
                    <a class="btn btn-white" href="{{ path('admin_countries_list', app.request.query.all) }}"><i class="fa fa-arrow-left" aria-hidden="true"></i> Powrót</a>
                    <button type="submit" class="btn-success btn">Zapisz</button>
                </div>
            </div>
        {{ form_end(form) }}

    </div>

</div>

{% endblock %}