{% extends 'backend/base.html.twig' %}

{% block pageContent %}
    
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">
            Nowy użytkownik
        </h1>
        <ol class="breadcrumb">
            <li>
                <i class="glyphicon glyphicon-dashboard"></i>  
                <a href="{{ path('admin_homepage') }}">Kokpit</a>
            </li>            
            <li>
                <i class="fa fa-user"></i>
                <a href="{{ path('admin_users_list') }}">Zarządzanie użytkownikami</a>
            </li>
            <li class="active">
                <i class="fa fa-file"></i>
                Nowy użytkownik
            </li>
        </ol>    
    </div>
</div>       

<div class="row">

    <div class="col-lg-12">
            
        {% form_theme form 'bootstrap_3_horizontal_layout.html.twig' %}

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="form-group">
                <div class="col-sm-offset-3 col-sm-9">
                    <button type="submit" id="brewery_save" class="btn-success btn">Zapisz</button>
                </div>
            </div>
        {{ form_end(form) }}

        <a href="{{ path('admin_users_list', app.request.query.all) }}"><span class="glyphicon glyphicon-arrow-left"> Powrót</span></a>

    </div>

</div>

{% endblock %}